{
  "platform": "android",
  "contract": "A Journey navigates between rendered screens within one Activity. Prepare a destination behind the outgoing screen before its dismissal checkpoint. Retain the outgoing screen on preparation failure or cancellation; unused destination resources drain before their artifact lease is released. Authored transition timing and checkpoint cancellation reconciliation require additional qualification.",
  "screens": [
    "screen_1",
    "screen_2",
    "screen_1"
  ],
  "activityLaunches": 1,
  "failedPreparation": {
    "dismissalCheckpoints": 0,
    "destinationLeaseReleased": true,
    "outgoingPixelsChanged": 0
  },
  "pendingCheckpointComposedPixelsChanged": 0,
  "checkpointRetry": {
    "contract": "A cancelled waiter does not cancel an admitted dismissal checkpoint. Retry of the same destination joins its result; a different destination is superseded rather than reusing the original checkpoint.",
    "destination": "screen_details",
    "result": "REJECTED",
    "checkpointCalls": 1,
    "launchCountBeforeResult": 1,
    "launchCountAfterResult": 1,
    "conflictingDestination": "screen_welcome",
    "conflictReason": "SUPERSEDED"
  }
}
